apiVersion: v1
kind: ServiceAccount
metadata:
  name: xfx-adapter
  namespace: xfx-adapter
  annotations:
    # Override with environment-specific IAM role via kustomize patches
    eks.amazonaws.com/role-arn: "arn:aws:iam::role/PLACEHOLDER"
