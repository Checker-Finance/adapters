apiVersion: apps/v1
kind: Deployment
metadata:
  name: b2c2-adapter
  namespace: b2c2-adapter
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: b2c2-adapter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: b2c2-adapter
    spec:
      serviceAccountName: b2c2-adapter
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: b2c2-adapter
      containers:
        - name: b2c2-adapter
          image: b2c2-adapter:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: health
              containerPort: 9050
              protocol: TCP
          envFrom:
            - configMapRef:
                name: b2c2-adapter-config
            - secretRef:
                name: b2c2-adapter-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: health
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
      restartPolicy: Always
