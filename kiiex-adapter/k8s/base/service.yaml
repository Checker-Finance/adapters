apiVersion: v1
kind: Service
metadata:
  name: kiiex-adapter
  namespace: kiiex-adapter
spec:
  selector:
    app.kubernetes.io/name: kiiex-adapter
  ports:
    - name: health
      port: 9070
      targetPort: health
      protocol: TCP
