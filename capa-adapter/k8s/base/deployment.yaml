apiVersion: apps/v1
kind: Deployment
metadata:
  name: capa-adapter
  namespace: capa-adapter
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: capa-adapter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: capa-adapter
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9060"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: capa-adapter
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: capa-adapter
      containers:
        - name: capa-adapter
          image: capa-adapter:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9060
              protocol: TCP
          envFrom:
            - configMapRef:
                name: capa-adapter-config
            - secretRef:
                name: capa-adapter-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
      restartPolicy: Always
