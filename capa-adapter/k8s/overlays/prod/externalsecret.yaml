apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: capa-adapter-secrets
  namespace: capa-adapter
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: SecretStore
  target:
    name: capa-adapter-secrets
    creationPolicy: Owner
    template:
      data:
        DATABASE_URL: "{{ .database_url }}"
        REDIS_URL: "{{ .redis_url }}"
        NATS_URL: "{{ .nats_url }}"
        LOG_LEVEL: "{{ .log_level }}"
  data:
    - secretKey: database_url
      remoteRef:
        key: prod/capa-adapter
        property: database_url
    - secretKey: redis_url
      remoteRef:
        key: prod/capa-adapter
        property: redis_url
    - secretKey: nats_url
      remoteRef:
        key: prod/capa-adapter
        property: nats_url
    - secretKey: log_level
      remoteRef:
        key: prod/capa-adapter
        property: log_level
